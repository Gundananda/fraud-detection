# 🕵️‍♂️ Fraud Detection (Random Forest + SMOTE)

This project performs fraud detection on financial transactions using:

- **Feature Engineering**  
- **SMOTE Oversampling**  
- **Random Forest Classifier**  
- **ROC–AUC, Confusion Matrix, 5-Fold CV**

⚠️ The dataset is **500MB**, so it cannot be uploaded to GitHub.  
Place it manually in:

